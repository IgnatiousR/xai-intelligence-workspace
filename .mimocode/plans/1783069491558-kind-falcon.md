# Implementation Audit: Plan 00 (Architecture) & Plan 01 (Setup & Design System)

## Summary

The project has been **scaffolded** correctly (Next.js 16 + all dependencies installed), but **almost none of the plan 01 design system work has been implemented** — the `src/` directory is still the default `create-next-app` template.

---

## Plan 00 — Architecture ✅ Scaffolded / ❌ Not Implemented

### ✅ What's done
- **Next.js 16.2.10** with TypeScript, App Router, `src/` directory
- **All required packages installed** in `package.json`:
  - `three` ^0.185.1, `@react-three/fiber` ^9.6.1, `@react-three/drei` ^10.7.7
  - `@types/three` ^0.185.0 (devDep)
  - `gsap` ^3.15.0, `@gsap/react` ^2.1.2
  - `motion` ^12.42.2
  - `clsx` ^2.1.1, `tailwind-merge` ^3.6.0
- **`tsconfig.json`**: `@/*` → `./src/*` path alias configured
- **No `tailwind.config.ts`** — correct for Tailwind v4

### ❌ What's missing
- **Entire folder structure** from Plan 00 § "Folder structure" is absent:
  - `src/components/` — none of the 5 section directories (layout/, hero/, insight-flow/, dashboard/, wow-moment/, ui/)
  - `src/hooks/` — no `useGsapScrollTrigger`, `useScrollProgress`, `usePointer`, `useReducedMotion`
  - `src/lib/` — no `gsap.ts`, `cn.ts`
  - `src/shaders/` — no `particleField.ts`, `morphCloud.ts`
  - `src/data/` — no `nav.ts`, `stages.ts`, `dashboard.ts`

---

## Plan 01 — Setup & Design System

### ✅ What's done
- Project scaffold created correctly (Step 1)
- All dependencies installed (Step 2)
- `tsconfig.json` paths configured (Step 3)
- No `tailwind.config.ts` file (correct for v4)

### ❌ What's missing

#### Step 4: Fonts — NOT DONE
- `layout.tsx` uses **Geist / Geist_Mono** (default template), NOT **Space_Grotesk / DM_Sans** as specified
- Font variables are `--font-geist-sans` / `--font-geist-mono`, not `--font-display` / `--font-body`
- Metadata still says "Create Next App"

#### Step 5: Tailwind v4 tokens — NOT DONE
- `globals.css` is still the **default Next.js template** (white background, Geist fonts, dark mode toggle)
- **Missing entirely** — the `@theme` block with project tokens:
  - Colors: `--color-bg` (#06060a), `--color-bg-el`, `--color-fg`, `--color-fg-m`, `--color-accent` (#c8ff00), `--color-accent-d`, `--color-card`, `--color-bdr`
  - Semantic: `--color-critical`, `--color-info`, `--color-success`
  - Fonts: `--font-display`, `--font-body`
- **Missing entirely** — global styles:
  - `body` background (`#06060a`), `overflow-x: hidden`
  - Custom scrollbar styles
  - `.text-gradient-accent` class
  - `.shadow-accent-glow` class
  - `.border-gradient-hover` + `::before` pseudo-element + `spin-border` keyframe
  - Noise overlay `body::after` SVG
  - `pulse-soft`, `float-dot` keyframe animations
  - `prefers-reduced-motion` media query

#### Step 7: Base UI primitives — NOT DONE
- `src/components/ui/Button.tsx` — **does not exist**
- `src/components/ui/Tag.tsx` — **does not exist**
- `src/components/ui/SectionHeader.tsx` — **does not exist**

#### `page.tsx` — NOT DONE
- Still contains default template content (Next.js logo, "To get started, edit the page.tsx file")
- Should compose the 5 sections in order per Plan 00

---

## Implementation Plan (what needs to be built)

### Phase A: Design System in `globals.css`
1. Replace entire `globals.css` content with the plan 01 § Step 5 spec:
   - `@theme` block with all color/font tokens
   - Global body/scrollbar styles
   - Utility classes (`.text-gradient-accent`, `.shadow-accent-glow`, `.border-gradient-hover`)
   - Noise overlay, keyframes, reduced-motion query

### Phase B: Fonts in `layout.tsx`
1. Replace `Geist` / `Geist_Mono` imports with `Space_Grotesk` / `DM_Sans`
2. Set variables to `--font-display` / `--font-body`
3. Apply on `<html>` element
4. Update metadata (title, description)

### Phase C: Create folder structure
1. Create all missing directories:
   - `src/components/{layout,hero,insight-flow,dashboard,wow-moment,ui}/`
   - `src/hooks/`
   - `src/lib/`
   - `src/shaders/`
   - `src/data/`

### Phase D: Base UI primitives
1. Create `src/components/ui/Button.tsx` with `primary` / `secondary` variants
2. Create `src/components/ui/Tag.tsx`
3. Create `src/components/ui/SectionHeader.tsx` with `label` / `title` / `subtitle` props

### Phase E: Lib utilities
1. Create `src/lib/cn.ts` (clsx + tailwind-merge helper)
2. Create `src/lib/gsap.ts` (registerPlugin ScrollTrigger, client-only)

### Phase F: Data files
1. Create `src/data/nav.ts`, `src/data/stages.ts`, `src/data/dashboard.ts`

### Phase G: Hooks
1. Create all 4 hooks: `useGsapScrollTrigger`, `useScrollProgress`, `usePointer`, `useReducedMotion`

### Phase H: Update `page.tsx`
1. Replace template with section composition (stubs initially, real components later)

---

## Verification

After implementation:
- `npm run dev` shows a **dark** page (#06060a background) with no console errors
- Font-family resolves to Space Grotesk (headings) / DM Sans (body) — not browser defaults
- Tailwind utility classes using new tokens (e.g., `bg-bg`, `text-accent`, `border-bdr`) render correctly
- Scrollbar is styled (5px, dark track)
- Noise overlay is visible as subtle texture
- Button primary renders with green accent + glow shadow
